'use strict';
/* global describe, it */

const assert = require('assert');

describe('OperationController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers.OperationController);
  });
});
