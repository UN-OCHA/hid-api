'use strict';
/* global describe, it */

const assert = require('assert');

describe('ServiceCredentialsController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['ServiceCredentialsController']);
  });
});
