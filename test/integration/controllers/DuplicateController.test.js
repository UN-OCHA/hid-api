'use strict'
/* global describe, it */

const assert = require('assert')

describe('DuplicateController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['DuplicateController'])
  })
})
