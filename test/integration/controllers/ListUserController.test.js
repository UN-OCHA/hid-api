'use strict';
/* global describe, it */

const assert = require('assert');

describe('ListUserController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['ListUserController']);
  });
});
