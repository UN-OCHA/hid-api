'use strict';
/* global describe, it */

const assert = require('assert');

describe('OutlookController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers.OutlookController);
  });
});
