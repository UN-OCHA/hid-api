'use strict'
/* global describe, it */

const assert = require('assert')

describe('TOTPController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['TOTPController'])
  })
})
