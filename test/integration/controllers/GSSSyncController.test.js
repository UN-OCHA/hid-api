'use strict';
/* global describe, it */

const assert = require('assert');

describe('GSSSyncController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers.GSSSyncController);
  });
});
