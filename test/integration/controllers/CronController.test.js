'use strict';
/* global describe, it */

const assert = require('assert');

describe('CronController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers.CronController);
  });
});
