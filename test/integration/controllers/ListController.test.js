'use strict';
/* global describe, it */

const assert = require('assert');

describe('ListController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['ListController']);
  });
});
