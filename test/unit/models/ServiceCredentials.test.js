'use strict';
/* global describe, it */

const assert = require('assert');

describe('ServiceCredentials Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ServiceCredentials']);
  });
});
