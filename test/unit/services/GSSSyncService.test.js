'use strict';
/* global describe, it */

const assert = require('assert');

describe('GSSSyncService', () => {
  it('should exist', () => {
    assert(global.app.api.services.GSSSyncService);
  });
});
