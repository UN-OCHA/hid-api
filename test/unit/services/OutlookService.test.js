'use strict';
/* global describe, it */

const assert = require('assert');

describe('OutlookService', () => {
  it('should exist', () => {
    assert(global.app.api.services.OutlookService);
  });
});
