<p>Dear <%= user.name %>,</p>

<p>As per our email 12 days ago, you have now been automatically checked out of <a href="<%= params.list.getAppUrl() %>"><%= params.list.name %></a> as it is 14 days passed the departure date that you set for the list <a href="<%= params.list.getAppUrl() %>"><%= params.list.name %></a>.</p>

<p>If you are still responding to an emergency in <a href="<%= params.list.getAppUrl() %>"><%= params.list.name %></a>, you will need to check-in again.</p>

<% include ../includes/footer_en %>
