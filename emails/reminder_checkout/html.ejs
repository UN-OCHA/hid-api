<p>Dear <%= user.name %>,</p>

<p>We are reaching out to remind you that in two days we will check you out of <a href="<%= params.list.getAppUrl() %>"><%= params.list.name %></a>,
  as per your specified departure date of <%= params.listUser.checkoutDate %>.</p>

<p>In case you would like to join the list again simply go to <a href="<%= params.list.getAppUrl() %>"><%= params.list.name %></a> and check in (you can do so through the arrow icon on top).</p>

<% include ../includes/footer_en %>
